#include"main.h"

/**
 * _islower - returns 1 if it is lowercase letter otherwise 0
 * Retutrn: 0 or 1 if it works correctly
 */

int _islower(int c)
{
	int i;

	i = _islower('c');
	if (i != 0)
		_putchar('1');
	else 
		_putchar('0');
}
