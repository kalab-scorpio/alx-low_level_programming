#include"main.h"

/**
 * _isalpha - print 1 if it is a letter and 0 otherwise
 * Retutrn: 0 or 1 if it works correctly
 */

int _isalpha(int c)
{
	int i;

	i = _isalpha('c');
	if (i != 0)
		_putchar('1');
	else
		_putchar('0');
}
