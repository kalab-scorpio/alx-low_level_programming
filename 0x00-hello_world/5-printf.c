#include<stdio.h>

/**
 * main - just prints "with proper grammar, but the outocme is a piece of art"
 * Return: 0 if all goes well
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
