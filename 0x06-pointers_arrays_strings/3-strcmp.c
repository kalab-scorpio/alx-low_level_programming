/**
 * _strcmp - function to compare two char arrays
 * @s1: first char array to be compared
 * @s2: second char array to be compared
 * Return: returns int
 */
#include<string.h>

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
